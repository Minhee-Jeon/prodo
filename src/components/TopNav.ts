import { Component } from '../core/core';

export default class TopNav extends Component {
  constructor() {
    super({
      tagName: 'header'
    });
  }
  render() {
    this.el.innerHTML = /* html */ `
    
    `
  }
}